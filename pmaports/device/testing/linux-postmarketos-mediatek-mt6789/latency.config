# GPU regulator/power-domain sleeps and compositor timers need sub-tick
# precision. Without high-resolution timers, HZ=250 rounds microsecond
# sleeps to 4 ms ticks, accumulating ~100 ms in the measured GPU wake path.
# Keep HZ, preemption, idle policy, clocks and electrical delays unchanged.
CONFIG_HIGH_RES_TIMERS=y
