# The DC-1 USB gadget's usb0 interface is the point-to-point debug link to the
# host (172.16.42.1/24, static, set by dc1-usb-gadget). NetworkManager must not
# try to DHCP it, or it will fight the static address.
[keyfile]
unmanaged-devices=interface-name:usb0
