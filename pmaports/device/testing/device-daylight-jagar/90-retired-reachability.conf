# Older initramfs images reinstall this unit on every boot. The package's
# tmpfiles rule creates the opt-out before basic.target (also in charging mode).
[Unit]
ConditionPathExists=!/etc/dc1/boot-watchdog.disabled
