[Service]
# LocalSearch applies Landlock before execing its extractor. On Alpine's
# usr-merged musl layout, make the loader use paths that the sandbox explicitly
# admits; this preserves both Landlock and the extractor's seccomp filter.
Environment=LD_LIBRARY_PATH=/usr/lib:/usr/local/lib
