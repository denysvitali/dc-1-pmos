# A pre-pin s2idle cycle completed on 2026-08-19, but suspend/resume and wake
# have not been accepted on the current build. Keep the sleep targets masked
# until that work is complete. Ignore the power/suspend keys in logind so its
# default HandlePowerKey action (poweroff) cannot fire and a key press cannot
# enter an unverified sleep path.
#
# Note this does NOT govern the power key under GNOME: gnome-shell-mobile grabs
# it as a mutter keybinding and handles it itself, which is what the gschema
# override next to this file is for. This still covers the greeter, sway, and
# any session that leaves the key to logind.
[Login]
HandlePowerKey=ignore
HandleSuspendKey=ignore
HandleHibernateKey=ignore
HandleLidSwitch=ignore
